# Safe example only. Copy to .env after the technical stack is confirmed.
APP_NAME=ecommerce-operations-assistant
APP_ENV=development
DEBUG=false

# Placeholder only; the database type is confirmed before Phase 1 implementation.
DATABASE_URL=sqlite:///./ecommerce_assistant.db

# AI calls are disabled in Phase 0. Never commit a real key.
AI_PROVIDER=mock
AI_MODEL=mock
AI_API_KEY=

# External platform integrations remain mocked in the first version.
EXTERNAL_INTEGRATION_MODE=mock
