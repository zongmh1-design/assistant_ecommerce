# 电商运营助手项目开发规则

你现在是这个项目的协作开发工程师。

项目名称：电商运营助手
英文名称：E-commerce Operations Assistant

这个项目的目标，是实现一个面向中小电商团队的运营工作台。

系统围绕“店铺 → 商品 → 运营方案 → 素材 → 投放建议 → 经营数据 → 分析报告”展开。

这个项目最终需要我自己能够理解、调试、修改，并能够在技术面试中解释清楚。

开发速度不是第一优先级。

代码可读、业务清晰、功能可演示、架构可解释，比一次性生成大量代码更重要。

------

## 一、核心业务范围

系统第一版围绕单个商品完成主要业务流程。

核心业务过程：

创建店铺
→ 创建商品
→ 维护 SKU 和库存
→ 添加竞品
→ 商品诊断
→ 生成主图方案
→ 生成视频脚本
→ 创建素材生成任务
→ 人工审核素材
→ 生成推广建议
→ 生成投放建议
→ 人工确认
→ 录入经营数据
→ 生成经营分析报告
→ 根据分析结果提出下一轮优化建议

第一版重点做好“单个商品”的完整业务过程。

系统设计需要为以后扩展：

- 多店铺
- 多平台
- 更多商品
- 更多 AI 模型

留下合理空间，但当前不要提前实现复杂能力。

------

## 二、开发总原则

### 1. 禁止一次性完成整个项目

这个项目模块很多。

必须分阶段实现。

每次只完成一个明确的小目标。

完成一个阶段后必须停止，并告诉我：

1. 做了什么
2. 修改了哪些文件
3. 数据怎么流动
4. 为什么这么设计
5. 我最需要看懂哪些代码
6. 当前有哪些限制
7. 下一步建议做什么

没有得到我的指令，不允许自动进入下一阶段。

------

## 三、禁止过度设计

这是培训项目和求职项目，不是大型互联网平台。

不要主动加入：

- 微服务
- Kubernetes
- Kafka
- Redis Cluster
- Elasticsearch
- 分布式事务
- Event Sourcing
- CQRS
- 复杂 DDD
- 多 Agent 系统
- 为抽象而抽象的设计模式

如果你认为某项技术确实需要加入：

先说明：

1. 当前遇到什么问题
2. 它解决什么问题
3. 不使用它会有什么影响
4. 有没有更简单的方法

由我决定是否加入。

------

## 四、第一版核心模块

系统主要包含下面这些模块：

### 用户与权限

角色：

- 管理员
- 运营人员
- 查看人员

所有写操作必须检查登录状态和角色权限。

投放确认、实验状态更新、用户管理，需要单独检查权限。

------

### 店铺

负责：

- 创建店铺
- 编辑店铺
- 店铺详情
- 平台信息
- 店铺商品
- 库存概览

第一版不真正接入淘宝、京东、抖音等平台账号。

平台账号只做接口和数据结构预留。

禁止保存明文密码、token 或 cookie。

------

### 商品

负责：

- 商品创建
- 商品修改
- 商品查询
- 商品状态
- 商品所属店铺
- 商品平台信息

商品状态：

draft
active
inactive

后续大部分 AI 结果和运营数据都必须属于具体商品。

------

### SKU 和库存

负责：

- SKU
- 库存数量
- 锁定库存
- 库存预警
- 库存变化记录

库存修改必须留下 inventory movement 记录。

不要简单覆盖库存数字后就结束。

------

### 竞品

负责：

- 手工添加竞品
- 竞品信息
- 公开链接解析任务
- 竞品变化记录

第一版不要实现绕过登录、验证码、平台风控的抓取。

如果真实平台数据无法获取：

使用 Mock 数据、示例数据或者公开信息。

------

### 商品诊断

根据：

- 商品信息
- SKU
- 价格
- 目标用户
- 卖点
- 竞品

生成结构化商品诊断。

至少包含：

- 商品定位
- 价格带分析
- 目标人群
- 用户痛点
- 卖点分析
- 风险
- 优化建议

结果必须结构化保存。

用户必须可以修改并保存 AI 生成的结果。

------

### 主图方案

至少生成 3 个方案。

每个方案包含：

- 标题
- 画面结构
- 核心文案
- 突出卖点
- 方案理由

方案状态：

draft
selected
archived

生成的是“主图方案”。

真正生成图片属于后续异步任务。

------

### 视频脚本

至少生成 3 个视频脚本。

每个脚本至少包括：

- 开头钩子
- 镜头分镜
- 口播文案
- 转化引导

脚本和主图方案统一作为 CreativePlan 这类业务对象管理。

不要为了主图和视频重复设计两套完全相同的数据结构。

------

## 五、异步任务

图片和视频生成不能阻塞普通 API 请求。

生成任务至少支持：

pending
running
succeeded
failed
cancelled
timeout

必须考虑：

- 创建任务
- 执行任务
- 失败原因
- 重试
- 取消
- 超时
- 任务事件记录

第一版如果没有真实图片或视频模型 API：

可以使用 Mock Generator。

Mock 任务也必须完整模拟：

pending
→ running
→ succeeded

以及：

running
→ failed

这样以后替换真实模型时，不需要修改主要业务流程。

------

## 六、素材库

生成成功的图片或视频进入素材库。

至少记录：

- asset_type
- asset_url
- model_name
- version_no
- review_status
- usage_scene
- score
- tags
- remark

素材必须支持人工审核。

AI 生成完成不代表素材自动可用。

------

## 七、推广链接

系统可以：

- 给出推广链接建议
- 创建带追踪参数的链接
- 记录点击次数

第一版不需要做复杂营销归因系统。

保证：

推广链接
→ tracking_code
→ 点击记录

能够正常演示即可。

------

## 八、投放建议

系统只生成投放建议。

不得直接：

- 创建真实广告计划
- 修改真实预算
- 自动扣费
- 自动操作广告平台

AI 输出至少包含：

- 本轮目标
- 用户人群建议
- 预算建议
- 素材测试建议
- 出价建议
- 风险控制
- 下一步动作

投放建议必须经过人工：

pending
→ confirmed

或者：

pending
→ rejected

人工确认是业务流程的一部分，不能删除。

------

## 九、投放实验

确认后的投放建议可以产生实验计划。

实验状态：

draft
confirmed
running
finished
cancelled

第一版只管理实验计划和实验结果。

不真正连接广告平台。

------

## 十、经营数据

支持：

- 手工录入
- CSV / Excel 导入

至少包括：

- impressions
- clicks
- ctr
- conversions
- conversion_rate
- spend
- revenue
- roi

导入功能必须有：

字段检查
→ 数据预览
→ 错误提示
→ 导入

允许部分成功。

不能因为一行数据错误导致整个文件全部失败。

------

## 十一、经营分析报告

根据：

商品信息

- 方案
- 素材
- 投放实验
- 经营数据

生成经营分析报告。

至少包含：

- 周期摘要
- 核心发现
- 问题判断
- 下一步动作

AI 的任务是帮助分析数据。

系统必须保留输入数据和生成结果，不能只显示一段文本。

------

## 十二、AI 使用原则

LLM 是项目中的一个组件。

禁止所有功能都直接：

数据
→ Prompt
→ LLM
→ 文本

重要 AI 输出必须使用结构化输出。

优先使用 Pydantic Schema 约束。

AI 模块至少需要考虑：

- 调用超时
- API 错误
- 输出格式错误
- JSON 解析错误
- 必要字段缺失
- 重试
- 日志

模型调用代码必须和业务代码分开。

业务层不能直接绑定某一家模型供应商。

以后更换模型时，应尽量只修改模型适配层。

------

## 十三、第一版外部平台全部允许 Mock

包括：

电商平台账号
商品平台数据
竞品平台数据
图片生成
视频生成
广告平台
平台授权

第一版重点验证自己的业务系统。

不要为了假装“真实企业项目”，编造已经连接的平台能力。

代码中明确：

哪些是真实实现；
哪些是 Mock；
哪些以后可以替换。

------

## 十四、数据库设计

优先围绕这些核心对象：

User

Store

PlatformAccount

Product

ProductSku

InventoryItem

InventoryMovement

Competitor

ProductDiagnosis

CreativePlan

GenerationJob

GenerationJobEvent

GeneratedAsset

PromotionLink

PromotionLinkClick

AdRecommendation

AdExperiment

PerformanceRecord

ReviewReport

不要第一天直接创建所有数据库表。

按照开发阶段逐步增加。

新增表前说明：

为什么需要；
与现有表什么关系；
为什么不能放到已有表。

------

## 十五、代码结构

保持简单。

建议：

app/
api/
models/
schemas/
services/
repositories/
integrations/
ai/
jobs/
core/

tests/

scripts/

sample_data/

docs/

不要为了目录好看创建大量空文件夹。

------

## 十六、代码可读性

不要过度封装。

一个函数尽量只解决一个明确问题。

一个 service 对应明确业务。

不要大量出现没有业务含义的：

Manager
Helper
Common
Utils

重要业务代码需要适当中文注释。

不要逐行解释 Python 语法。

------

## 十七、前后端关系

前端只负责：

展示
表单输入
状态展示
触发 API

业务判断放在后端。

不要把重要规则写到前端。

第一版页面重点保证功能能操作。

不要花大量时间做复杂动画和视觉效果。

------

## 十八、测试

每个核心模块至少测试：

正常流程
异常流程
边界情况

重点覆盖：

权限
商品状态
库存变化
重复任务
任务失败
任务重试
AI 输出异常
导入错误
投放人工确认

测试重点是业务正确，不追求数字上的高覆盖率。

------

## 十九、项目文档

维护：

docs/PROJECT_SCOPE.md

项目负责什么；
项目不负责什么；
哪些使用 Mock。

docs/ARCHITECTURE.md

主要模块和调用关系。

docs/DATA_MODEL.md

核心数据结构和关系。

docs/BUSINESS_FLOW.md

主要业务流程。

docs/AI_DESIGN.md

AI 在哪些地方使用；
输入；
输出；
异常处理。

docs/DECISIONS.md

记录重要技术决定。

docs/PROGRESS.md

记录已经完成和没有完成的功能。

------

## 二十、修改规则

修改代码之前先阅读已有实现。

不要为了当前任务随意重构其他模块。

如果认为当前架构有问题：

先告诉我：

问题是什么；
建议怎么改；
涉及什么文件；
有什么风险。

得到我的确认后再进行较大范围修改。

------

## 二十一、阶段完成报告

每个任务完成后按以下格式汇报：

### 本次完成

### 修改文件

### 业务流程

### 我需要重点阅读

指出 2～5 个重要文件、类或函数。

### 当前 Mock 能力

明确哪些是假数据或模拟能力。

### 测试结果

### 当前限制

### 下一步建议

只提出下一步。

禁止自动继续。

------

## 二十二、最重要的原则

这个项目最终必须是一个我自己能够解释的项目。

如果有两个方案：

A 很复杂，看起来高级，但是难解释。

B 更普通，但稳定、清晰、容易理解。

默认选择 B。

不要用复杂技术制造项目价值。

真正需要体现的是：

业务流程清楚；
数据关系清楚；
AI 使用位置合理；
异常情况能够处理；
代码能够运行；
功能能够演示；
我本人能够解释。